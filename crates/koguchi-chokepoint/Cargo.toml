[package]
name = "koguchi-chokepoint"
version = "0.1.0"
edition = "2021"
description = "Rust chokepoint spike for Koguchi/CAH — not a security sandbox"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
